#include "main.h"
/**
 * _abs - calculates the absolute value of an integer
 * @n: input number
 * Return: the exit value of the program
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
