#include <stdio.h>
/**
 * main - main entry point of the program
 * return: exit status of the program
 */

int main(void)
{
	char letter;
	for (letter = 'a'; letter <= 'z'; letter++)
	{
		putchar(letter);
		putchar('\n');
	}


return(0);
}
